{
  "name": "music-note-playground",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:unit": "npm run test"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^8.0.16",
    "vitest": "^4.1.9"
  }
}
